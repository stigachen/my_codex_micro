<Project Sdk="Microsoft.NET.Sdk">
  <!-- Platform-neutral logic: protocol decoding, config, shortcut grammar, mapping.
       Builds and tests on any OS, so the Windows port can be developed on a Mac. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>MicroKeys.Core</RootNamespace>
  </PropertyGroup>
</Project>
